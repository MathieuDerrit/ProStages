{% extends 'base.html.twig' %}

{% block titrePage %}Stage(s) de l'entreprise {{entreprise.nom}} {% endblock %}

{% block contenuPage %}
<div class="table-responsive">
  <table class="table table-striped table-bordered">
    <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Titre</th>
          <th scope="col">Adresse</th>
          <th scope="col">Formation</th>
          <th scope="col">Email</th>
        </tr>
    </thead>
    <tbody>
        {% for stageCourant in entreprise.stages %}
          {# on rajoute une ligne pour chaque stage #}
          <tr>
            <th scope="row">{{loop.index}}</th>
            <td>{{stageCourant.titre}}</td>
            <td>{{entreprise.adresse}}</td>
            <td>{% for formation in stageCourant.formations %}
                  {{formation.titre }} .
                {% endfor %}</td>
            <td>{{stageCourant.email}}</td>
          </tr>

  {% endfor %}
    </tbody>
  </table>
</div>
{% endblock %}
